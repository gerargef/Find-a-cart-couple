// import { describe, expect, test } from "@jest/globals";
// import { createCardField } from "../card-field";
// describe("Card-field", () => {
//   describe("createCardField()", () => {
//     test("it should array which length is cards quantity", () => {
//       const cardsQuantity = 6;
//       const cardSrcArray = createCardField(cardsQuantity);
//       expect(cardSrcArray).toHaveLength(cardsQuantity);
//     });
//   });
// }); 
